<%= turbo_stream.append dom_id(leaf, :being_edited) do %>
  <%= tag.div id: dom_id(user, :being_edited_by), class: "being-edited-by", data: { controller: "autoremove", action: "animationend->autoremove#remove", hide_from_user_id: user.id } do %>
    <%= user.name %> is also editing this right now
  <% end %>
<% end %>


